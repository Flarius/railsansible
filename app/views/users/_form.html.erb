<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario %><br>
    <%= f.text_field :user, :required => true, :placeholder => 'ingrese un usuario' %>
  </div>
  <div class="field">
    <%= f.label :contraseña %><br>
    <%= f.password_field :pass, :required => true, :placeholder => 'ingrese una contraseña'  %>
  </div>
  <div class="field">
    <%= f.label ("repetir contraseña") %><br>
    <%= f.password_field :pass, :required => true, :class => 'passControl', :placeholder => 'repita su contraseña'%>
  </div>  

   <div class="field">
     <%= f.submit :crear, :id => 'botonControl', :disabled => 'true' %> 
   </div> 
<% end %>
